<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.vrforum.web.mapper.UserMapper">
	<select id="selectUser" resultType="UserVO">
		SELECT
			*
		FROM
			`user`

	</select>
	
	<select id="selectUserinfo" resultType="UserVO">
				SELECT
			idx, id
		FROM
			`user`
		WHERE
			id = #{userId} AND password = #{password}
	</select>
	
	<insert id="insertUser">
		INSERT INTO
		<choose>
			<when test="#{hasVr}">
 				`user` (id, password, has_vr, vr)
			VALUES
				(#{userId}, #{password}, #{hasVr}, #{vr})
			</when>
			<otherwise>
				`user` (id, password, has_vr)
			VALUES
				(#{userId}, #{password}, #{hasVr})
			</otherwise>
		</choose>	
	</insert>
</mapper>